{
  "name": "social-wiki-kernel",
  "description": "The frame and Graffiti connection kernel for Social.Wiki",
  "scripts": {
    "build:types": "tsc --declaration --emitDeclarationOnly",
    "build:js": "tsx esbuild.config.mts",
    "build": "rm -rf dist && npm run build:types && npm run build:js",
    "check": "tsc --noEmit",
    "prepublishOnly": "npm run check && npm run build"
  },
  "dependencies": {
    "@graffiti-garden/api": "^1.3.0",
    "@graffiti-garden/wrapper-data-guard": "^0.1.0",
    "@graffiti-garden/wrapper-iframe-rpc": "^0.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
